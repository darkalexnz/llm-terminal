# task-plans/

Workspace-level task planning only. One file per task: `plan-[task-name].md`.

Each plan file contains both the Research and Plan phases in a single document, following the Task Execution Protocol in `/WORKSPACE.md`.

**Not for project-specific task planning** — that goes in the project's own `tier-3-work/` directory.
